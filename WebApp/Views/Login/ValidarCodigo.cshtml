@{
    Layout = "~/Views/Shared/_LayoutLogin.cshtml";
    ViewData["Title"] = "Validação de Código";
}

<div class="telas-inicias">
    <h1>Validação de Código</h1>
    <p>Para continuar, por favor, informe o código enviado para o seu email.</p>

    <form id="formValidarCodigo">
        <div class="input-telas-inicias">
            <input type="text" id="codigo" name="codigo" required placeholder="Código de validação" maxlength="6" />
        </div>

        <div id="loadingCodigo" class="loading">
            <div class="spinner"></div>
            <div class="loading-text">Validando código...</div>
        </div>

        <div class="botoes-telas-inicias">
            <button type="button" id="btnenviacodigo">Validar Código</button>
            <a href="@Url.Action("Index", "Login")">Voltar</a>
        </div>

        <div id="mensagemCodigo" class="alert alert-info" style="display: none;"></div>
    </form>
</div>