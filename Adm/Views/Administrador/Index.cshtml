@model Adm.ViewModel.AdministradorIndexViewModel

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Painel do Administrador";
}

<link rel="stylesheet" href="~/css/pages/Administrador/administrador.css" />

<div class="admin-dashboard">
    <h1>Painel do Administrador 📊</h1>

    <div class="dashboard-container">

        <div class="dashboard-card">
            <h2>Estatísticas de Artistas</h2>
            <table class="tabela-estatisticas">
                <thead>
                    <tr>
                        <th>Artista</th>
                        <th>Músicas Publicadas</th>
                        <th>Total de Reproduções</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var artista in Model.EstatisticasArtistas)
                    {
                        <tr>
                            <td>@artista.NomeArtista</td>
                            <td>@artista.TotalMusicas.ToString("N0")</td>
                            <td>@artista.TotalReproducoes.ToString("N0")</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <div class="dashboard-card">
            <h2>Músicas Mais Populares</h2>
            <table class="tabela-estatisticas">
                <thead>
                    <tr>
                        <th>Título</th>
                        <th>Artista</th>
                        <th>Reproduções</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var musica in Model.EstatisticasMusicas)
                    {
                        <tr>
                            <td>@musica.Titulo</td>
                            <td>@musica.NomeArtista</td>
                            <td>@musica.TotalReproducoes.ToString("N0")</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

    <div class="exibir-usuarios">
        <h2>Gerenciamento de Usuários</h2>
        <table class="tabela-usuarios">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Email</th>
                    <th>Tipo</th>
                    <th>Ações</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var usuario in Model.Usuarios)
                {
                    <tr>
                        <td>@usuario.Id</td>
                        <td>@usuario.Email</td>
                        <td>@usuario.Tipo</td>
                        <td>
                            <a class="botao-excluir" href="@Url.Action("Excluir", "Administrador", new { id = usuario.Id })"
                               onclick="return confirm('Tem certeza que deseja excluir o usuário @usuario.Email?');">
                                Excluir
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>